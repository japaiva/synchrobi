# api/urls.py - URLs da API REST do SynchroBI

from django.urls import path
from . import views

app_name = 'api'

urlpatterns = [
    # APIs futuras serão adicionadas aqui
    # Exemplo:
    # path('status/', views.status, name='status'),
]